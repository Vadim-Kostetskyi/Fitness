import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  noMarginBottom: {
    marginBottom: 0,
  },

  background: {
    minHeight: "100%",
  },

  buttonBox: {
    alignItems: "center",
    paddingTop: 250,
  },

  trainingChoose: {
    width: 200,
    marginBottom: 200,
  },
});
