import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  noMarginBottom: {
    marginBottom: 0,
  },

  background: {
    minHeight: "100%",
  },

  homeChoose: {
    width: 200,
    marginBottom: 100,
  },
});
